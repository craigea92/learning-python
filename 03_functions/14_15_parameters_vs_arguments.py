"""
PARAMETERS VS ARGUMENTS

Parameters are the names used for inputs when defining a function. Arguments are the names of the inputs supplied when a function is called.

To reiterate, arguments are the actual values that go into the function, say 42, "the dark knight" or True.
Parameters are the names we use in the function definition to refer to those values, which at the time of writing the function, could be anything.

# a and b are parameters
def add(a, b):
    return a + b

# 5 and 6 are arguments
sum = add(5, 6)
"""

# Question 01

# _____ are the values that are passed into the function by the caller

# Parameters
# Arguments

# Answer = Arguments

# Question 02

# _____ are the inputs specified bu the function definition

# Parameters
# Arguments

# Answer = Parameters